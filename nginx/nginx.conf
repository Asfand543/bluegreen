upstream backend {
    server localhost:8081;  # Blue
    # server localhost:8082;  # Green (switch manually or via Jenkins)
}

server {
    listen 80;
    location / {
        proxy_pass http://backend;
    }
}
